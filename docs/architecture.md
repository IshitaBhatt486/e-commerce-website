MVC architecture: Model View Controller

# Folder Structure
Backend:
- config: configurations related to DB, etc
- controller: main logic handling
- middleware: guard- checks the incoming/outgoing requests
- model: database, schemas, format of data
- routes: paths
- utils: utilities- sending a mail, tempelates